# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

## Project

- Product name: 手工川扫描
- Runtime target: Expo Go on iOS, Expo SDK 57
- Keep document processing local to the device.
- Keep the capture-to-export workflow usable without accounts or a backend.
- Prefer Expo Go-compatible packages; native modules that require a custom development build need an explicit product decision.
- Run `npm run typecheck`, `npm run doctor`, and `npx expo export --platform ios` before merging.
